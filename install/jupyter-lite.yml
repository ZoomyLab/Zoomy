name: zoomy_lite

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - pip:
      - jupyterlite>=0.4.1
      - jupyter-lite-pyodide-kernel
      - jupyter-server  # optional for icons/indexing
