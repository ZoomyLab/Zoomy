[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "zoomy-client"
version = "0.1.0"
description = "Lightweight HTTP client for Zoomy solver servers"
requires-python = ">=3.10"
dependencies = []
